# 目标配置示例；当前 CLI 尚未读取此文件。
[research_budget]
max_notes = 200
max_comments = 500
max_model_calls = 30
max_input_tokens = 200000
max_output_tokens = 20000
max_retries = 2

[metrics]
minimum_cohort_size = 20
like_weight = 0.25
save_weight = 0.45
comment_weight = 0.30

[execution]
online_sources_enabled = false
cloud_model_enabled = false
export_requires_version_approval = true
