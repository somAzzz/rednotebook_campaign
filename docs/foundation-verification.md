# S1–S3 本轮验收记录

验收日期：2026-09-17（Europe/Berlin），版本 v0.2.0。运行原始 UTC 时间及代码/依赖/夹具哈希见 [机器记录](foundation-verification.json)。代码尚未提交 Git，因此以源码树 SHA-256 标识本轮版本。

## 实际结果

- `uv sync --locked`：通过。
- `uv run pytest -q`：**48 个测试通过**。
- `uv run ruff check .` 与 `uv run ruff format --check .`：通过。
- `uv run rednotebook doctor`：Python 3.12.13、SQLite 3.53.1，检查通过。
- `uv run python scripts/verify_foundation.py`：合成端到端、数据完整性和性能检查通过。
- `uv run python scripts/check_scaffold.py`：CLI 状态与文档本地链接检查通过。

F0 共 705 行输入：702 行有效、3 行无效（JSON 项 703–705；CSV 记录 704–706）。导入保存 200 篇笔记、500 条评论、603 个指标快照。重复导入的新增证据/修订/指标均为 0。197 篇满足可比条件，另外 3 篇分别因近似精度单独成组不足、缺指标、缺账号规模而不给分。

参考机器 `macOS-26.6.2-arm64-arm-64bit` 上，合成生成、JSON+CSV 导入、覆盖、排序、撤销和数据库完整性检查合计 **0.181 秒**，当前进程峰值 RSS **46.2 MiB**，满足本阶段 60 秒/1GiB 工程目标。这不包含模型推理或真实采集。

## 用例对应与边界

| 用例 | 本轮结果 | 限制 |
|---|---|---|
| A01 | 契约校验通过 | 真实 CanDo 功能与素材未核验 |
| A02 | unknown/denied、到期和用途检查通过 | 许可内容由本地操作者提供；尚无云端模型接入 |
| A03–A06 | 去重、历史、行级错误、原子回滚及覆盖统计通过 | 中断后整批重跑，无逐行断点；不是实际平台评论完整性验证 |
| A07–A09 | 手算分位、n=19/20、缺失/精度/分组及差值测试通过 | 不提供真实 CTR；没有真实数据效果结论 |
| A16 | 数据库撤销、到期拒读、清理与墓碑测试通过 | 未覆盖尚未实现的模型缓存、派生研究报告和图文包生命周期；完整用例仍未放行 |
| A12（局部） | 恶意评论只作为数据，离线链无网络调用 | 研究 Agent/工具网关尚不存在，完整提示注入测试待 S4 |
| A10–A15 其余、A17–A21 | 未执行 | 属于后续 Agent、创意、审核、导出与真实试点 |

额外回归：历史补导不覆盖较新观察；数值与原始显示矛盾拒绝；同一采样 run 不可改群体；同观察截断标记不可改写；错误报告不回显输入值；较新数据库版本拒绝打开修改。

## 阶段决定

本地数据基础可进入 S4 开发，**不代表 S1 真实资料基线、S4 研究准确率或完整 MVP 验收通过**。未连接账号/模型、未自动发布、未确认真实创作收益。

进入模型能力实测所需：本地服务地址及模型名；真实研究质量评估还需获准 F1 样本和人工标注。CanDo 实际功能与截图作为后续创意阶段的事实依据。
