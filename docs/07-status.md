# 从候选扫描到精读与策划

2026-09-17，v0.9.0，SQLite 10。

## 默认流程

用户要求“先研究再策划”时，先用 `plan_search` 和 `search_with_plan` 扫描约30–40篇去重候选，再由调用模型结合用户目标选择3–5篇，调用 `research_topic` 逐篇打开正文、采集图片/评论、分析图片、导入及研究。选篇理由是待验证判断，不能仅按搜索卡片显示数字取前五。

搜索数量配置保持原样：核心查询默认20，最多3个默认扩展各5，总计35个候选槽位；去重后实际数量可能不足30。报告实际数量与覆盖缺口，不循环搜索凑数。精读默认3–5篇；可提供 `sample_exception` 解释为何只有1–2篇。每篇保存 `reason`、`question` 和 `coverage`，单篇问题追加到研究问题中。不同笔记使用独立研究 brief，避免后一篇研究混入前面已导入的样本。

只搜索时可以停在候选列表；只用作者材料创作时不需要浏览器或研究。搜索阶段保留 `metric_display` 原值及每次查询命中，不自动解释成赞藏量或稳定热度排名。

## MCP 接口

1. 读取 `rednotebook://schemas/topic-selection` 和 `rednotebook://schemas/brief`。
2. 调用 `research_topic(source_id, selection, brief)`。`selection` 指向同一来源的已执行完毕 `search_plan` 任务，并选择其中的笔记ID。程序从保存结果解析采集地址，不接受任意替换地址。
3. 用 `get_job(include_result=true)` 读取进度。结果分别报告 `search_complete`、`candidate_count`、`selected_count`、`completed_count`、`research_complete`、`campaign_ready` 和实际研究记录ID。`notes` 保留入选理由、问题、子任务ID及 `research_brief`，完成子任务包含 `reading_scope`、图片分析和研究结果。
4. 完成后读取各篇研究及引用，比较共同点、分歧、限制和证据空白。若内容与选择时的判断不符，可解释原因并重新选择，不把标题匹配当作正文相关性证明。
5. 创建 ContentBrief 时设置 `research_mode="research_then_plan"` 和 `topic_job_id`。程序自动接入各篇研究、来源和 bundle 依赖。缺少或未完成精读的此模式会被拒绝。
6. 生成并审阅策划，核对作者目标、研究支持、重复免责声明和面向读者的表达。创作提示词要求跨篇比较，但语义质量仍需人工复核；不会因为结构通过就宣称研究准确或文案有效。

`capture_images`、`analyse_images` 默认 true，`max_images` 默认20，评论上限默认5。按目标调整读取范围：图片包含主要正文时保留图片分析，研究读者问题时采集评论。读取范围会保存，关闭图片分析不能声称已读完图片。所有笔记都沿用本地 deep 预算，默认每篇60次模型请求、4,000,000输入预算，不因批量精读压缩单篇预算。

独立的搜索辅助创作使用 `research_mode="search_only"` 和 `search_job_id`，搜索标题、原始显示指标及命中信息进入有来源依赖的上下文，不包含签名采集地址。作者材料模式使用 `author_only`。为兼容旧 brief，缺省 `optional` 保留旧行为；新 Skill 和 MCP 说明要求根据用户任务显式选模式，程序不能仅凭自然语言判定用户原本要求什么。

## 停止与恢复

明确识别的视频返回 `skipped/video_deferred`，不采集或研究视频，继续余下图文候选；记录 `skipped_video_count`，不计入精读完成数，需补选时新建选择。其他子任务失败、暂停或部分完成时，批量任务立即停止，不继续后续笔记，也不自动重复采集。已完成任务保留；父任务取消会取消当前子任务，已保存材料保留。浏览器暂停处理规则与现有单篇流程一致，topic 操作和续跑不会复位暂停。

读取结果的 `stopped_job_id`。可以用该条 `research_brief` 和已保存 `capture_id` 显式恢复单篇 `research_workflow`，或在必要时由用户明确要求重新采集。允许部分材料生成的研究仍可保持partial，它可以支持受限报告，但不能把缺失阅读算作完整样本。单篇恢复完成后，调用：

```text
resume_topic_research(
  job_id="停止的主题任务ID",
  replacement_jobs={"笔记ID": "完成的恢复子任务ID"}
)
```

续跑验证来源、原研究 brief 与笔记身份；重用已完成子任务，只执行尚未开始的条目。服务重启后的父任务也可按此方式恢复。不能通过 `retry_job` 从头重跑主题任务。新选择创建新主题任务；目前不同选择之间不自动复用子任务，避免把不匹配的问题或阅读范围当成已完成。

## 存储、迁移与撤销审查

显式9→10迁移只登记新JSON契约版本，不重写旧任务、旧bundle payload、hash和批准状态；旧程序会拒绝新数据库版本。主题任务与子任务仍保存在来源绑定的 `browser_jobs` 中，选择理由、问题、读取范围和进度随来源撤销/到期清除。策划从主题任务解析实际研究ID并写入既有 `bundle_runs`/`bundle_sources`，依赖旧机制清理所有版本及受管导出。搜索辅助上下文也登记来源。

本次没有创建额外不受管理的研究副本。签名链接仅在已有来源任务请求/搜索结果中用于采集，不进入主题结果或策划上下文。手工另存报告和模型端点自身的请求留存仍不属于数据库清理范围。

## 验证与限制

离线临时数据库测试覆盖：35篇合成候选、3篇真实代码路径采集/导入/研究（使用确定性模型替身）、策划研究依赖、来源撤销、第二篇失败停止、显式恢复不重复第一篇、部分材料不能冒充精读完成、跨来源与候选身份校验、暂停不复位、父子取消及重启读取、9→10旧payload不变，以及MCP工具实际调用。

这不是在线选样质量、真实图片阅读质量或文案传播效果验收。本轮不访问真实账号、不读取真实研究数据库。部署后需重启MCP服务以发现新工具；首次用新版打开数据库会执行迁移。搜索、正文与评论始终是有界样本；已读原文仍可能只是作者未经核实的自述。

验证命令：`uv sync --locked`、`uv run pytest -q`（183 passed）、`uv run ruff check .`、`uv run ruff format --check .`（95 files）、`uv run python scripts/check_scaffold.py`（57个链接）、`uv run python scripts/verify_foundation.py` 均通过；研究与策划两个Skill通过quick_validate。测试使用临时库和合成材料，不代表真实内容质量通过。

本轮186项离线测试通过（14.43秒）；ruff检查、格式、50个当前文档链接与基础验证通过。基础验证0.660秒、峰值124.2MB、700条合成记录撤销清理通过。未宣称真实平台视频识别或禁播在线验收完成。


## 2026-09-18 完整采集验收标准

新增[自动检索与研究审核标准](18-mcp-review-acceptance.md)。最终精读样本要求完整标题/正文、全部图片及逐图读取；评论允许少量有依据的讨论样本。明确独立完整性门槛、故障用例、真实逐页核对及当前实现差距。本轮仅制定文档，未修改采集代码，未执行真实完整采集验收。

## 2026-09-18 浏览器超时与恢复核查

修复open、renderer探活、逐标签页禁播注入、状态读取、recover-ui及清理等待缺少整体期限的问题，增加保守的挂起/时钟跳变检测。新增浏览器生命周期Skill说明并同步本机版本；未发现报告所述当前Skill断链。193项离线测试及格式、链接、基础验证通过。历史系统睡眠不是本次已独立复现的唯一根因，详情见[超时核查记录](19-browser-timeout-review.md)。未解除真实浏览器暂停或操作账号。


## v0.10.0 采集审计修复

新增身份和完整性门槛、标题回填标记、默认5条评论及可读互动指标、图文优先和评论局部故障隔离、检查点续采、CLI零图片及安全阶段日志。SQLite10→11保留旧payload，检查点和事件沿用来源清理。核查结论、修复与限制见[采集审计修复](20-capture-audit-fixes.md)。未操作真实账号或改写历史研究结果；真实全图采集和OCR准确性仍待验收。

本轮全量206项离线测试通过；最后的落盘门槛调整另有71项相关回归通过。格式、文档链接、Skill和基础验证通过。已同步本机研究Skill，新增工具/契约需重启MCP后使用。

## 2026-09-18 pipeline 复核修复

确认四项问题：分页仅直接点击分页点、缺图时默认推荐allow_partial、恢复动作与暂停/主题结果冲突、最终查询进度少计一条。另已将本文评论默认上限改为5。

分页新增悬停显示控件、可交互子控件及下一页箭头路径；箭头仅在独立页码可确认当前位置时使用，逐步核对页码。下载仍要求资源已变化，并在存在页码指示时核对目标页；错页/旧图不计入完整采集。不强制点击、不解除暂停、不调用页面内部轮播API。未知控件形态仍会安全失败。

缺图默认动作改为 `resume_collect_images`，对应 `resume_collect(capture_id=..., resume_from="images")`；补齐后用新采集任务ID继续工作流。正文/身份问题需检查后显式重采，图片读取失败先重试gallery。`allow_partial` 仅保留为用户明确选择的受限报告路径，不计入完整精读。`get_job` 的恢复提示统一考虑暂停复位白名单和结果内动作，包括旧记录中的partial提示；查询每次成功追加后同步进度。

本次不改变持久化字段或数据库版本，不改写历史任务；动作提示和进度沿用既有来源绑定、撤销和保留规则。全量离线回归209项通过，随后新增错页阻断回归1项通过；`uv sync --locked`、ruff检查/格式、文档链接及基础验证通过。新增17页合成DOM覆盖悬停控件、不可点击分页点加箭头及错误页码；没有取得本次失败的真实DOM或重跑真实17图笔记，因此线上阻断是否解除仍待复测，docs/18真实完整流程验收仍未通过。


## 2026-09-18 五场景真实MCP审核

完成五种模拟用户场景，见[审核报告](21-mcp-five-scenario-audit.md)与[机器核验](mcp-five-scenario-verification.json)。确认真实根因为后台Chromium动画帧停滞，另修复Vue ref元数据读取、轮播克隆图片签名及话题编码比较。17图样本最终续采成功，另完成两主题各3篇纯采集；合计7篇48图，无缺页，逐文件哈希/解码复核通过。搜索分别10、31、26候选，原词及4查询进度一致；缺图导入与不完整研究策划均正确拒绝。

用户明确要求暂不进行本地模型测试，后续未再调用模型；本次没有图片文字准确率、研究语义或docs/18完整精读验收结论。真实采集通过不等于研究完成。模型不可连接的早期尝试还暴露了逐页重复请求和进度少计问题，已补充安全错误码、共享故障立即停止及成功页计数，使用离线替身回归。

220项离线测试、依赖锁定、ruff检查/格式、文档链接及基础验证通过。未变更持久化字段或版本，不改写旧任务，不扩大授权，不操作发布或互动。后续优先完善页位资源审计、长图文字核对和评论加载状态区分。

## 2026-09-18 Codex助手替代模型复测

用户明确授权本次7篇由Codex分析后，新增显式、采集版本绑定的助手接入路径；原来源云端总权限不变，没有自动云端回退。实际读取7篇正文、48图及22条评论，保存7条研究/21条发现；两个3篇主题与独立17图单篇主题进入策划，各生成6页v2草案及未批准预览。38处引用校验通过，未批准正式导出正确拒绝。未调用本地模型、未发布。

SQLite11→12登记助手提交契约，旧grant/payload保持不变，派生内容沿用来源保留和撤销清理。功能链路通过不等于docs/18严格人工准确性验收；打码、小字、未与现场逐段核对等边界见[助手pipeline复测](22-assistant-pipeline-verification.md)。

本轮最终224项离线测试通过；锁定依赖、ruff检查/格式、72文档链接及基础验证通过。

## 2026-09-19：调用方 agent 双路径

新增 caller_analysis：证据/评论分页、原始 MCP ImageContent、逐页摘记导入、固定引用校验与外部结论提交；纯采集主题不会调用本地模型，也不提前放行 campaign。支持调用方策划语义审阅，以及单进程 loopback Streamable HTTP 多客户端共享；STDIO 与本地模型路径保留。schema13 保持历史授权与证据不变，旧 Codex 单次授权不扩展到其他处理方。协议与合成端到端验证见 [docs/23](23-agent-neutral-mcp.md)，不替代真实人工精度验收。

双路径回归：230 passed；新增调用方/旧助手相关10项通过；lint、format、scaffold 和 foundation 验证通过。未运行真实本地模型，未把合成结果计为真实研究验收。
