# MCP 调研能力实测

2026-09-17，v0.5.1。用户已明确恢复测试；保持固定浏览器会话，页面间隔60秒、动作间隔3秒，只选一篇，不在异常后转向下一篇。视频继续暂缓。

## 验收结论

MCP搜索、已有真实材料的本地模型研究、任务资源读取、草稿创建/修订、六页预览，以及未审核导出拦截均通过功能验证。新笔记的整篇多图采集尚未通过；研究结论的正式语义准确率也未通过。

## 本轮网页访问

- 使用MCP `browser_status` 确认当前会话显示logged_in。
- `search_notes` 搜索“AI 简历”，限制1个候选、0次滚动；得到[一键生成瑞典风格求职简历](https://www.xiaohongshu.com/explore/6a631d0b0000000002003c00)，检索链接包含访问参数。搜索返回partial是有界采样标记，不代表搜索失败。
- 间隔至少60秒后，`collect_note` 成功打开该笔记详情。页面存在正文和14条加载的评论，但采集任务没有完成导入，不能将其计为已采集材料。
- 翻页定位失败：页面有两个分页容器，可见media容器和隐藏teleport副本。旧代码点击隐藏副本，最终任务失败；系统持久暂停，未自动转向下一篇。
- 本地修复为优先选取笔记内可见media分页，缺少时才选可见teleport分页；新增回归测试。只读检查当前页面时确认8个目标控件均可见。没有继续点击或重跑采集，所以不宣称整篇图片采集修复已实测通过。

搜索任务：`ef80822c-6d5c-4d59-920e-57b9976df614`。
采集尝试：`fd8d77a0-eaf9-4fb3-b24b-610525926f1e`。

## MCP 模型研究与交付

使用此前已保存、已入库的3篇真实公开图文笔记和30条评论（其中一篇带8图机器提取）。这是独立验证下游能力，不把旧材料冒充本轮自动采集结果。

| 工具/能力 | 结果 |
|---|---|
| research_notes → get_job | 完成，3篇/30评论，1批，3条带引用观察 |
| 本地模型 | qwen3.8-27b，3次调用；服务报告输入55474、输出1578 tokens |
| rednotebook://jobs/{id} | 可读取，run_id与任务结果一致 |
| propose_draft | 创建确定性六页编辑框架，draft v1；没有冒充模型生成的最终成稿 |
| revise_draft | 修订成v2，仍为draft，不继承批准 |
| preview_draft | 六张1080×1620 PNG及正文、来源、manifest；已做六页目视检查 |
| export_draft（未批准版本） | 正确拒绝：exact_version_approval_required |

研究任务：`d24e3eca-514b-493e-bd37-e7edc4ed5f31`。
研究run：`41643961-5f31-46dc-9c81-cd6ffa55406b`。
草稿：`f62095a4-fc3e-4232-b231-9018f1f63529`，最新预览v2。

本地详细调用记录在Git忽略的 `private/browser-probe/saved-research-mcp-report.json`；预览在 `private/pilot/managed-exports/f62095a4-fc3e-4232-b231-9018f1f63529-v2-preview/`，检查图为 `private/browser-probe/mcp-preview-reviewed.png`。文件快照仍遵循原先说明的保留范围，研究数据库中的结果仍绑定来源许可。

## 研究质量抽查

以下是助手抽查，不是独立人工标注，也不用于宣称F1正式验收通过。

1. AI简历笔记的评论存在JD含义、招聘方是否使用AI筛选等明确问题；模型还关联了关键词评论和图片中的互动引导。但输出把media_text中的图片文字称为“正文”，来源表述不准确。
2. 食物笔记评论含辨认内容、请求教程及发酵说明；模型将发酵陈述也列作问题例子，并扩展到未被所列引用直接支持的“口感”提问。引用位置正确不等于研究判断正确。

修订预览明确区分图片文字、提问、观点与制作说明，并保留小样本、机器识别待核验等限制；原始研究结果未覆盖改写，仍标记hypothesis/semantic_review=pending。

## 工程回归与后续

113项离线测试通过，Ruff检查与格式检查通过。搜索结果现明确区分可展示的
`public_url` 与只供采集的 `collect_url`；研究引用由程序补充来源链接并在 Markdown
中呈现，缺失链接时不会猜测。新增隐藏分页副本回归用例。下一轮真实采集需要在明确恢复后，从同一篇笔记验证可见翻页和图片顺序；无需再做批量搜索。正式大样本研究准确率、原生纯文字形态和长期效果仍未验收。
